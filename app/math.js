
module.exports = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,
    //Advanced
    sqrt: (num) => Math.sqrt(num),
    max: (a, b) => Math.max(a, b)
  };
  